The indentation got messed up. Replace the entire file with this exact content:

```yaml
name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Capacitor
        run: |
          npm init -y
          npm install @capacitor/core @capacitor/android
          npm install -g @capacitor/cli

      - name: Init Capacitor
        run: |
          npx cap init "VG Studio" com.iamthevgod.vgstudio --web-dir www
          mkdir www
          cp index.html www/index.html

      - name: Add Android
        run: |
          npx cap add android
          npx cap sync android

      - name: Clean Gradle Cache
        run: |
          rm -rf ~/.gradle/caches

      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: vgstudio-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
```

Select all and delete everything first, then paste this clean version and commit.
